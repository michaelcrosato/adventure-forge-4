node_modules/
runs/
*.log
# region authors' in-progress part files; landed regions move up to world/reach/
world/reach/wip/
node_modules

# agent worktrees (ephemeral; a stray `git add -A` would otherwise commit a whole checkout)
.claude/worktrees/
